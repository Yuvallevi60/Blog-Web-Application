<!DOCTYPE html>
<html lang="en" class="container">

<head>
    <meta charset="UTF-8">
    <meta name="Blog" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <title>Blog</title>
</head>

<body>

  <div class="container">
    <div class="web-title">
      <a href="/">My Blog</a>
    </div>
    <div class="nav-bar">
        <% if (locals.posts) { %>
          <% for (let i=0; i<posts.length; i++) { %>
              <form action="/postpage" method="POST">
                  <input type="hidden" name="pIndex" value="<%= i %>">
                  <button type="submit" class="nav-bar-button"><%= posts[i]["pHead"] %> </button>    
              </form>
          <% } %>
        <% } %>     
    </div>